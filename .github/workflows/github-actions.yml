name: GitHub Actions Demo
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Running on a ${{ runner.os }}"
      - name: Run on Google Chrome
        run:
      - run: echo "🍏 This job's status is ${{ job.status }}."
